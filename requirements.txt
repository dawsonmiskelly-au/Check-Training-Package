# Step 1 — Install Unsloth first. It manages torch, transformers, and bitsandbytes internally.
# Do NOT install torch separately — it will conflict.
# Run this first:
#   pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"

unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git

# Step 2 — Install these with --no-deps so they don't override Unsloth's managed versions:
#   pip install --no-deps trl peft accelerate
trl
peft
accelerate

# Step 3 — General dependencies (safe to install normally):
Pillow
sentencepiece
protobuf
huggingface_hub
hf_transfer      # optional but speeds up model downloads from HuggingFace on AWS

# System dependencies (not pip — must be installed on the machine):
# - git        (for cloning llama.cpp during GGUF export)
# - cmake      (for building llama.cpp quantize tool)
# - gcc/g++    (C++ compiler for llama.cpp)
# - NVIDIA CUDA toolkit (for GPU training)
